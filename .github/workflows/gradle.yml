name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: 11
      - name: Build with Gradle
        run: |
             - echo '${{ secrets.PRIVATE_KEY }}' > key
             - ssh -i key valera@34.88.77.186
             - ${{ secrets.PASSWORD_TO_CLOUD }}
             - docker start my_postgres
             - docker ps
             - ip address show
             - exit